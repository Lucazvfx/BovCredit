# Versões fixas, não faixas.
#
# gestao_model.pkl é um pickle do scikit-learn. Pickle não atravessa versão de
# biblioteca. O modelo deve ser regenerado quando estas versões mudarem.
# Versões fixas evitam que cada ambiente resolva dependências diferentes.
#
# Ao subir qualquer versão aqui, regenere o modelo (python treinar_ciclos.py)
# e rode a suíte antes de commitar.
flask==3.1.3
# O gestao_model.pkl versionado foi gerado com scikit-learn 1.5.2 e numpy 1.26.4.
scikit-learn==1.5.2
numpy==1.26.4
pdfplumber==0.11.10
# Já vinha como dependência transitiva do pdfminer.six, mas agora o segredo
# TOTP é cifrado com Fernet — depender disso por acaso, no caminho de
# autenticação, é o tipo de coisa que quebra numa atualização do pdfplumber.
cryptography==50.0.0
openpyxl==3.1.5
gunicorn==26.0.0
psycopg2-binary==2.9.12
joblib==1.5.3
flask-login==0.6.3
xgboost==3.2.0
apscheduler==3.11.3
requests==2.33.1
lightgbm==4.7.0
reportlab==5.0.0
# SHAP 0.51 exige numpy>=2; o modelo versionado usa numpy 1.26.4.
shap==0.49.1
flask-limiter==4.1.1
redis==5.2.1
groq==1.6.0
# Ponte para o binário do Tesseract (instalado no Dockerfile). Só o wrapper: se
# o binário não estiver na imagem, `import pytesseract` passa e a chamada é que
# falha — por isso os dois têm de andar juntos.
pytesseract==0.3.13
